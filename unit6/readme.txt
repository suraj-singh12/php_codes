Truncate:
does not allows data to be rolled back, deletes permanently

Delete:
allows data to be rolled back even after deleting

commit;
let's store data permanently at the database.

rollback;
let's rollback data to the previous state.

// commnad: TRUNCATE table tableName
